base = int(input("Insert the base of the triangle (using cm): "))
height = int(input("Insert the height of the triangle (using cm): "))

area = base * height / 2

print(f"The area of the triangle is {area:.2f}cm")
